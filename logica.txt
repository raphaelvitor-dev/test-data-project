FILE_DOWNLOADER:

parsea a lista de hrefs de arquivos zips de tras pra frente (o beautiful soup adiciona a lista na ordem do html)

sera gerado uma lista com o nome de cada zip. Porem para a requisicao e download, precisamos automatizar e gerar uma lista
com as urls com o nome do arquivo no final para requisicao.

se a lista ficar com menos de 3 arquivos:
	

	volta um diretorio diminuindo 1 unidade no ano no final da url
	acessa o ano anterior (max_year - 1)
	baixa o ultimo arquivo de tras pra frente tambem do ano anterior


Adiciona os arquivos baixados a uma pasta com formato ANO/QQ


Para extrair os arquivos:
	funcao extract_files é criada, parsea os anos dentro da pasta trimestre e pra cada ano dentro de trimestre, parsea as pastas dentro do ano. Apos isso
	Vai extrair os 3 arquivos de tras pra frente (do recente ao antigo).

Normalizacao de dados 9 passos:
	1 - ler arquivos com diferentes extensoes .csv, .xlsx, .txt

	2 - Padronizacao de Colunas | df.columns.str.strip().str.lower()

	3 - Remover duplicatas | df.drop_duplicates()

	4 - Remover colunas Vazias | df.dropna(axis=1, how"all")

	5 - Tratar os tipos de dados | df.info() pd.to_{datatype}(colunadesejada, errors="coerce"(forca a transformar null em data)

	6 - Tratar LINHAS com valores vazio | tabela["colunadesejada] = df["colunadesejada].fillna(valordesejado)

	7 - Padronizar Valores de Texto | df["colunadesejada].lower() ou .upper() dependendo do dado

	8 - Padronizar decimais e floats | tabela["valores"] = tabela["valores"].replace("R$","").replace("." , "").replace("," , ".")

	9 - Criar colunas auxiliares | tabela["novacoluna"] = expressao